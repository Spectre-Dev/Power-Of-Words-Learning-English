<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
android:orientation="vertical"
android:layout_width="match_parent"
android:layout_height="match_parent">

<Button
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="right"
    android:text="Sign Out"
    android:id="@+id/signOutBtn"
    android:onClick="signOut"
    android:textColor="#ffffff"
    android:layout_marginTop="5dp"
    android:layout_marginRight="5dp"
    android:background="@drawable/buttonstyle" />

<TextView
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:textAppearance="?android:attr/textAppearanceMedium"
    android:text="Please select a game to play:"
    android:id="@+id/messageTv"
    android:layout_gravity="center_horizontal"
    android:layout_marginTop="10dp"
    android:textColor="#003366" />

<ScrollView
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center_vertical" >

        <ImageButton
            android:id="@+id/listenAndSpellBtn"
            android:layout_width="fill_parent"
            android:layout_height="130dp"
            android:scaleType="fitXY"
            android:text="Listen and Spell"
            android:onClick="listenAndSpellGame"
            android:textColor="#ffffff"
            android:layout_marginBottom="10dp"
            android:layout_gravity="center_horizontal"
            android:background="#00000000"
            android:layout_marginTop="10dp"
            android:src="@drawable/listenandspell" />

        <ImageButton
            android:id="@+id/hangmanBtn"
            android:layout_width="fill_parent"
            android:layout_height="130dp"
            android:scaleType="fitXY"
            android:text="Hangman"
            android:onClick="startHangman"
            android:background="#00003366"
            android:textColor="#ffffff"
            android:layout_gravity="center_horizontal"
            android:src="@drawable/hangman" />

        <ImageButton
            android:id="@+id/matchBtn"
            android:layout_width="fill_parent"
            android:layout_height="130dp"
            android:scaleType="fitXY"
            android:text="Word Match"
            android:onClick="startWordMatch"
            android:background="#00000000"
            android:textColor="#ffffff"
            android:layout_marginTop="10dp"
            android:layout_gravity="center_horizontal"
            android:src="@drawable/wordmatch" />


    </LinearLayout>

</ScrollView>

</LinearLayout>

